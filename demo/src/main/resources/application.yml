server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springsecurity?serverTimezone=Asia/Seoul
    username: root
    password: '0000'

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update   # 처음엔 update, 나중에 validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
  
  mvc: 
    hiddenmethod:
      filter:
        enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: kmh000904@gmail.com
    password: minhh5009894!!
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
